[package]
name = "northstar-logging"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "North Star structured logging bridge over ULOG sinks."

[lib]
name = "northstar_logging"
path = "src/lib.rs"

[dependencies]
northstar-ulog = { path = "../northstar_ulog" }
serde_json = "1.0"
