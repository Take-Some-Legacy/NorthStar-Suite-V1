# GNU mkdir

Pinned local vendor copy from `C:\msys64\usr\bin\mkdir.exe`. Active payload lives under `tools/toolbelt/third_party/mkdir`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
