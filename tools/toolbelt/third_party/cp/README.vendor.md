# GNU cp

Pinned local vendor copy from `C:\msys64\usr\bin\cp.exe`. Active payload lives under `tools/toolbelt/third_party/cp`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
