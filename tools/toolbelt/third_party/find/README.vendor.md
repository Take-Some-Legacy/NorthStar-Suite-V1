# GNU find

Pinned local vendor copy from `C:\msys64\usr\bin\find.exe`. Active payload lives under `tools/toolbelt/third_party/find`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
