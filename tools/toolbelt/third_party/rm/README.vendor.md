# GNU rm

Pinned local vendor copy from `C:\msys64\usr\bin\rm.exe`. Active payload lives under `tools/toolbelt/third_party/rm`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
