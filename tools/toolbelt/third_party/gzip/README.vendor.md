# GNU gzip

Pinned local vendor copy from `C:\msys64\usr\bin\gzip.exe`. Active payload lives under `tools/toolbelt/third_party/gzip`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
