# GNU xargs

Pinned local vendor copy from `C:\msys64\usr\bin\xargs.exe`. Active payload lives under `tools/toolbelt/third_party/xargs`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
