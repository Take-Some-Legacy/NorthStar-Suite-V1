# GNU mv

Pinned local vendor copy from `C:\msys64\usr\bin\mv.exe`. Active payload lives under `tools/toolbelt/third_party/mv`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
