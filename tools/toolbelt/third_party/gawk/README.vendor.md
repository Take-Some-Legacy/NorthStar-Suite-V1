# GNU gawk

Pinned local vendor copy from `C:\msys64\usr\bin\gawk.exe`. Active payload lives under `tools/toolbelt/third_party/gawk`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
