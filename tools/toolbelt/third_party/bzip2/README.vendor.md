# GNU bzip2

Pinned local vendor copy from `C:\msys64\usr\bin\bzip2.exe`. Active payload lives under `tools/toolbelt/third_party/bzip2`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
