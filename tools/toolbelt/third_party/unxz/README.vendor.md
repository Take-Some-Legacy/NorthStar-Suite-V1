# GNU unxz

Pinned local vendor copy from `C:\msys64\usr\bin\unxz.exe`. Active payload lives under `tools/toolbelt/third_party/unxz`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
