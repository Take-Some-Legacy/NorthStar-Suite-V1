# dos2unix

Pinned local vendor copy from `C:\Program Files\Git\usr\bin\dos2unix.exe`. Active payload lives under `tools/toolbelt/third_party/dos2unix`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
