# GNU xz

Pinned local vendor copy from `C:\msys64\usr\bin\xz.exe`. Active payload lives under `tools/toolbelt/third_party/xz`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
