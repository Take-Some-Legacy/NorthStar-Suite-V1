# GNU awk

Pinned local vendor copy from `C:\msys64\usr\bin\awk.exe`. Active payload lives under `tools/toolbelt/third_party/awk`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
