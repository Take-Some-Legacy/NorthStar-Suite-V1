# GNU patch

Pinned local vendor copy from `C:\Program Files\Git\usr\bin\patch.exe`. Active payload lives under `tools/toolbelt/third_party/patch`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
