# unix2dos

Pinned local vendor copy from `C:\Program Files\Git\usr\bin\unix2dos.exe`. Active payload lives under `tools/toolbelt/third_party/unix2dos`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
