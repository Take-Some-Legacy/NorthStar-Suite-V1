# GNU file

Pinned local vendor copy from `C:\msys64\usr\bin\file.exe`. Active payload lives under `tools/toolbelt/third_party/file`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
