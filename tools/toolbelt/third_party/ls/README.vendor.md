# GNU ls

Pinned local vendor copy from `C:\msys64\usr\bin\ls.exe`. Active payload lives under `tools/toolbelt/third_party/ls`. Runtime DLLs are resolved through `vendor.msys2.runtime.usr` in `tools/toolbelt/libraries`.
