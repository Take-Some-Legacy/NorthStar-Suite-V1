# Test data: northstar.neui_packer

Purpose: deterministic local smoke/proof inputs for `northstar.neui_packer`.

`test/test.bat` writes proof artifacts to `test/_out/`.

Keep this directory small and self-contained.
