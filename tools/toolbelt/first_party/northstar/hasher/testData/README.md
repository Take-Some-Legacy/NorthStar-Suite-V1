# North Star Hasher Test Data

`names.txt` is a small deterministic smoke-test input for `northstar-hasher`.

It exercises:

- default lowercase hashing;
- literal hashing;
- extension stripping;
- optional hash filtering.
