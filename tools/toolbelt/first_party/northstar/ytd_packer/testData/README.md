# Test data: northstar.ytd_packer

Purpose: deterministic local smoke/proof inputs for the first-party YTD packer package.

The default smoke test validates package wiring and executable help output. Format-specific pack/unpack test data can be added here without changing the root `testAll.bat` runner.

Known source formats for this tool:

```text
PNG
BMP
JPG
JPEG
DDS
TGA
```

Explicitly not source formats:

```text
XSR
EXR
```
