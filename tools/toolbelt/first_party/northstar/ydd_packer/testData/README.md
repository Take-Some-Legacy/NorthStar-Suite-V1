# Test data: northstar.ydd_packer

Purpose: deterministic local smoke/proof inputs for `northstar.ydd_packer`.

`test/test.bat` writes proof artifacts to `test/_out/`.

Keep this directory small and self-contained.
